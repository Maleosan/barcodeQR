export function inventoryCsv(items) { const quote=v=>`"${String(v??'').replaceAll('"','""')}"`; const rows=[['Kode','Nama','Kategori','Satuan','Lokasi','Stok','Minimum','Status'],...items.map(i=>[i.code,i.name,i.category,i.unit,i.location,i.stock,i.minimumStock,i.stock===0?'HABIS':i.stock<=i.minimumStock?'MENIPIS':'AMAN'])]; return '\ufeff'+rows.map(row=>row.map(quote).join(',')).join('\n'); }
export function downloadCsv(items) { const url=URL.createObjectURL(new Blob([inventoryCsv(items)],{type:'text/csv;charset=utf-8'})); const anchor=document.createElement('a'); anchor.href=url; anchor.download=`laporan-stok-${new Date().toISOString().slice(0,10)}.csv`; anchor.click(); setTimeout(()=>URL.revokeObjectURL(url),500); }
